{
    "compilerOptions": {
      // target the modern JS runtime you’re using
      "target": "ES2020",
  
      // resolve modules like Node does
      "module": "ESNext",
      "moduleResolution": "node",
  
      // turn off type-checking if you just want JS (optional)
      "checkJs": false,
  
      // only include Node’s global types, not the DOM
      "types": ["node"]
    },
    "exclude": ["node_modules"]
  }
  